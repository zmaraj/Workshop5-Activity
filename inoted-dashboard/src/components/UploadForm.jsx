// components/UploadForm.jsx
// This form lets a student upload a note.
//
// WHAT TO DO IN THIS FILE:
//   - Create a component that returns a form container (a styled div)
//   - Add a text input for the note title with a label
//   - Add a dropdown (select) to pick the class with a label
//   - Add a file upload area — style a <label> to look like
//     a button and hide the real <input type="file"> with className="hidden"
//     clicking the label will open the file picker automatically
//   - Add an "Upload Note" button aligned to the right
//
// HINT: In React, use htmlFor instead of for on labels
// HINT: The label's htmlFor and the input's id must match

export default function UploadForm() {
  return (
    <div>
      {/* your form content goes here */}
    </div>
  );
}